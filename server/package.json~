{
  "name": "server",
  "version": "1.0.0",
  "description": "Server to make dark sky api requests",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tarik Courdy",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.3"
  }
}
